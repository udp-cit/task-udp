#include <iostream>
#include <string>
using namespace std;

void genera_premios(string x[20]) {
    // Esta función genera un arreglo de premios aleatorio.
    // 18 premios, 1 comodín y 1 agujero-negro.
    // Mezclamos el arreglo de premios
}

string validaJuego(string x[20], int pos1, int pos2) {
    if (x[pos1] == "Agujero-negro" || x[pos2] == "Agujero-negro") {
        return "Muerte";
    }
    if (x[pos1] == "Comodín") {
        return x[pos2];
    }
    if (x[pos2] == "Comodín") {
        return x[pos1];
    }
    if (x[pos1] == x[pos2]) {
        return x[pos1];
    }
    return "Siga participando";
}

int main() {
    string premios[20];
    genera_premios(premios);

    int intentos = 3;
    bool juegoTerminado = false;

    cout << "¡Bienvenido al juego de los pares: Desafía tu mente!" << endl;

    while (intentos > 0 && !juegoTerminado) {
        int pos1, pos2;
        cout << "Ingrese la primera posición (0-19): ";
        cin >> pos1;
        cout << "Ingrese la segunda posición (0-19): ";
        cin >> pos2;

        if (pos1 < 0 || pos1 >= 20 || pos2 < 0 || pos2 >= 20 || pos1 == pos2) {
            cout << "Posiciones inválidas. Intente de nuevo." << endl;
            continue;
        }

        string resultado = validaJuego(premios, pos1, pos2);
        cout << "Resultado: " << resultado << endl;

        if (resultado == "Muerte") {
            cout << "¡Perdiste el juego!" << endl;
            juegoTerminado = true;
        } else if (resultado != "Siga participando") {
            cout << "¡Ganaste con el premio: " << resultado << "!" << endl;
            juegoTerminado = true;
        } else {
            cout << "Siga participando..." << endl;
        }

        intentos--;
    }

    if (!juegoTerminado) {
        cout << "Se acabaron los intentos. ¡Gracias por jugar!" << endl;
    }

    return 0;
}